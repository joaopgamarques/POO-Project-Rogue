package pt.iscte.poo.example;

// The Enemy interface serves as a marker for all enemy-type game elements.
// Classes implementing this interface represent entities that act as opponents
// to the hero within the game.

// This interface can be used to group and identify all enemy-related entities,
// making it easier to filter and handle enemies during gameplay logic (e.g., movement, attacks).
public interface Enemy {
    // No methods or fields are defined here, as this interface is a marker.
}
